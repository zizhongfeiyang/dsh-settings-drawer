/**
 * Settings drawer host half.
 * UI lives in ./client.js; this empty apply exists so the plugin appears
 * in the web profile Loader.
 */
export const name = 'dsh-settings-cleaner'

export function apply() {}
